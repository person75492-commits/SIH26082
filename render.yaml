services:
  - type: web
    name: aeris-72-aqi
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard/app.py
    envVars:
      - key: PORT
        value: 10000
